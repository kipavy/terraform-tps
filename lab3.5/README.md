Lab 3.5
Fetch and use AMI via datasource
Fetch and use subnet via datasource
Display as a variable output the public DNS
Display as a variable output the public IP
Make it a module with name and instance type configurable